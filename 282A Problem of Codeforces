#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    int n,count=0;
    string s;
    cin>>n;
    while(n--){
        cin>>s;
        if(s[0]=='+' || s[2]=='+')
            count++;
        if(s[0]=='-' || s[2]=='-')
            count--;
    }
    cout<<count<<endl;
 
    return 0;
}
